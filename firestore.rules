rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Exact Admin UID for now.
    // In future, this will be replaced by "deny all" after moving to Admin SDK.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
